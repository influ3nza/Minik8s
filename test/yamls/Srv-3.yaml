apiVersion: v1
kind: Service
metadata:
  labels:
    app: test
  name: srv-3
  nameSpace: q
spec:
  ports:
    - name: port1
      protocol: TCP
      port: 10000
      targetPort: 8080
      nodePort: 30000
  selector: 
    app: test
  type: NodePort
