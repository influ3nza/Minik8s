apiVersion: v1
kind: Service
metadata:
  labels:
    app: test-hpa
  name: srv-hpa
  nameSpace: q
spec:
  ports:
    - name: port1
      protocol: TCP
      port: 10000
      targetPort: 80
      nodePort: 30000
  selector: 
    app: test-hpa
  type: NodePort
